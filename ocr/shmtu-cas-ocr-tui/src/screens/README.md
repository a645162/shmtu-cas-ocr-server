## src/screens/

FTXUI screen components.

- `tag_list.{h,cpp}` — release tag browser (left panel).
- `model_list.{h,cpp}` — model table + metrics (middle panel).
- `download_progress.{h,cpp}` — download gauge + status (right/bottom panel).
- `download_dialog.{h,cpp}` — modal engine/precision selector.
